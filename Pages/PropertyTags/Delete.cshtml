@page
@model Swapartment.Pages_PropertyTags.DeleteModel

@{
  ViewData["Title"] = "Delete";
}

<div class="bg-image d-flex justify-content-center align-items-center mb-3 p-5">

  <div class="border shadow p-5 height-auto">

    <div class="d-flex flex-column height-100">

      <div class="mb-5">
        <h2>Are you sure you want to delete @Html.DisplayFor(model => model.PropertyTag.Name)?</h2>
      </div>
      <dl class="row">
        <dt class="col-sm-2">
          @Html.DisplayNameFor(model => model.PropertyTag.Name)
        </dt>
        <dd class="col-sm-10">
          @Html.DisplayFor(model => model.PropertyTag.Name)
        </dd>
        <dt class="col-sm-2">
          @Html.DisplayNameFor(model => model.PropertyTag.IconUrl)
        </dt>
        <dd class="col-sm-10">
          @Html.DisplayFor(model => model.PropertyTag.IconUrl)
        </dd>
      </dl>

      <form method="post" class="mb-3">
        <input type="hidden" asp-for="PropertyTag.Id" />
        <input type="submit" value="Delete" class="btn btn-primary" />
      </form>
      <div>
        <a asp-page="./Index">Back to List</a>
      </div>
    </div>
  </div>

  <div>
